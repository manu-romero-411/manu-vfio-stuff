# WARNING DE KVM AL RESPECTO DE ignored rdmsr

Abrir /etc/modprobe.d/kvm.conf y poner:

options kvm report_ignored_msrs=0

# BLACKLISTEAR NOUVEAU

sudo bash -c "echo blacklist nouveau > /etc/modprobe.d/blacklist-nvidia-nouveau.conf"
sudo bash -c "echo options nouveau modeset=0 >> /etc/modprobe.d/blacklist-nvidia-nouveau.conf"
sudo update-initramfs -u

# RED PUENTEADA

Montarla con virt-manager. Es más sencillo

## LO QUE DEJÉ HECHO EN FEBRERO DE 2022

* Abrir ssh desde otro lado
* pci-unbind
* vm-windows
* Cuando se acabe todo, pci-rebind hace volver al escritorio
